[project]
name = "podcastify-monorepo"
version = "0.0.0"
description = "Dev tooling for Podcastify monorepo (services and libs live in subprojects)."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
